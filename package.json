{
  "name": "nba-tracker",
  "version": "1.0.0",
  "description": "NBA game tracker PWA — follow your teams & find where to watch",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
